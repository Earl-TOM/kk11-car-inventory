# Project Rules — kk11-car-inventory

## Auto Push to GitHub
After every code change or set of changes, always stage all modified files and push to GitHub:

```bash
git add -A
git commit -m "<descriptive message>"
git push origin main
```

Do this automatically without waiting for the user to ask.
