# --------------------------------------------
# Server-only secrets (DO NOT prefix with VITE_)
# --------------------------------------------

# Neon Postgres connection string
DATABASE_URL=

# Neon Auth upstream base URL (server-only)
NEON_AUTH_BASE_URL=

# --------------------------------------------
# Client-visible variables (only if truly needed)
# Any key prefixed with VITE_ is exposed publicly.
# --------------------------------------------
# VITE_PUBLIC_APP_NAME=AutoTrade Inventory